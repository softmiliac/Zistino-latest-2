# MeliPayamak Troubleshooting Guide

## ❌ Error: RetStatus 9 - InvalidNumber

اگر با خطای `{"Value":"5","RetStatus":9,"StrRetStatus":"InvalidNumber"}` مواجه شدید، این راهنما را دنبال کنید:

### 🔍 علل احتمالی:

1. **شماره فرستنده (Sender) فعال نیست**
   - به پنل MeliPayamak بروید: https://panel.payamak-panel.com/
   - بخش "شماره فرستنده" یا "Sender Numbers" را بررسی کنید
   - مطمئن شوید شماره `21700077` فعال و تایید شده است
   - اگر شماره فرستنده ندارید، باید یکی درخواست دهید

2. **محدودیت IP**
   - در پنل MeliPayamak، بخش "تنظیمات وب‌سرویس" یا "Web Service Settings" را باز کنید
   - بخش "آیپی‌های مجاز" را بررسی کنید
   - اگر IP شما در لیست نیست، باید اضافه کنید
   - برای تست محلی، می‌توانید لیست IP را خالی بگذارید (اما در production حتما IP سرور را اضافه کنید)

3. **موجودی حساب کافی نیست**
   - در پنل MeliPayamak موجودی حساب را بررسی کنید
   - اگر موجودی ندارید، باید شارژ کنید

4. **شماره گیرنده نامعتبر است**
   - شماره `09056761466` را بررسی کنید که معتبر باشد
   - سعی کنید با شماره دیگری که مطمئن هستید معتبر است تست کنید

5. **API Key یا Username اشتباه است**
   - مطمئن شوید `MELIPAYAMAK_USERNAME` و `MELIPAYAMAK_API_KEY` در `.env` درست هستند
   - API Key باید از بخش "توسعه دهندگان > تنظیمات وب‌سرویس" در پنل گرفته شود

### ✅ راه‌حل‌های پیشنهادی:

#### 1. بررسی در پنل MeliPayamak:

```
1. وارد پنل شوید: https://panel.payamak-panel.com/
2. به بخش "تنظیمات" یا "Settings" بروید
3. بخش "شماره فرستنده" را بررسی کنید
4. مطمئن شوید شماره `21700077` فعال است
5. بخش "آیپی‌های مجاز" را بررسی کنید
6. IP سرور خود را اضافه کنید (یا برای تست خالی بگذارید)
7. موجودی حساب را بررسی کنید
```

#### 2. تست با شماره دیگری:

شماره `09056761466` ممکن است مشکل داشته باشد. سعی کنید:
- با شماره خودتان که در MeliPayamak ثبت شده تست کنید
- با شماره دیگری که مطمئن هستید معتبر است تست کنید

#### 3. تماس با پشتیبانی MeliPayamak:

اگر همه چیز درست است اما هنوز خطا می‌گیرید:
- با پشتیبانی MeliPayamak تماس بگیرید
- خطای `RetStatus: 9` را به آنها گزارش دهید
- از آنها بپرسید چرا شماره فرستنده `21700077` کار نمی‌کند

### 📞 اطلاعات تماس پشتیبانی:

- وب‌سایت: https://panel.payamak-panel.com/
- تلفن پشتیبانی: معمولا در پنل موجود است

### 🔧 تست سریع:

برای تست سریع، می‌توانید از اسکریپت تست استفاده کنید:

```bash
cd zistino_backend_api/zistino_backend
python test_melipayamak_api.py
```

این اسکریپت تمام فرمت‌های ممکن را تست می‌کند و به شما می‌گوید کدام یک کار می‌کند.

### ⚠️ نکته مهم:

خطای `RetStatus: 9` معمولا به این معنی است که:
- شماره فرستنده (`from`) فعال نیست، یا
- شماره گیرنده (`to`) نامعتبر است، یا
- محدودیت IP وجود دارد

باید در پنل MeliPayamak این موارد را بررسی کنید.

