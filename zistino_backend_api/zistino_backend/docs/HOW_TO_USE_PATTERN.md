# راهنمای استفاده از Pattern برای ارسال پیامک

## 📋 چرا Pattern؟

- ✅ **ارزان‌تر:** Pattern معمولاً 50-100 تومان هزینه دارد (پیامک آزاد 80-120 تومان)
- ✅ **قابل اعتمادتر:** Pattern تایید شده است و تحویل بهتری دارد
- ✅ **بدون نیاز به فعال‌سازی شماره فرستنده:** Pattern از شماره فرستنده استفاده نمی‌کند

## 🚀 مراحل استفاده:

### مرحله 1: ثبت Pattern در MeliPayamak

1. وارد پنل MeliPayamak شوید: https://panel.payamak-panel.com/
2. به بخش **"الگوها"** یا **"Patterns"** بروید
3. روی **"ایجاد الگوی جدید"** کلیک کنید
4. Pattern را به این صورت ثبت کنید:

**عنوان Pattern:**
```
اعلان عمومی
```

**متن Pattern:**
```
{0}
```

**توضیحات:**
- `{0}` به معنای اولین آرگومان است
- این Pattern ساده‌ترین حالت است و هر متنی را می‌پذیرد

5. Pattern را ثبت کنید و منتظر تایید بمانید (معمولاً 1-2 روز کاری)

### مرحله 2: دریافت Pattern ID

بعد از تایید Pattern:
1. در لیست Pattern ها، ID Pattern را پیدا کنید (مثلاً `389656`)
2. این ID را یادداشت کنید

### مرحله 3: اضافه کردن به .env

فایل `.env` را باز کنید و این خط را اضافه کنید:

```env
MELIPAYAMAK_PATTERN_ID=389656
```

(عدد `389656` را با Pattern ID خودتان جایگزین کنید)

### مرحله 4: ری‌استارت سرور

```bash
# Stop server (Ctrl+C)
# Then restart:
python manage.py runserver
```

### مرحله 5: تست

حالا می‌توانید از پنل React اعلان بفرستید. سیستم به صورت خودکار:
1. ابتدا Pattern را امتحان می‌کند
2. اگر Pattern کار نکرد، به پیامک آزاد برمی‌گردد

---

## 📝 مثال Pattern های دیگر:

### Pattern برای اعلان سفارش:
```
عنوان: اعلان سفارش
متن: کاربر گرامی، {0}
```

### Pattern برای اعلان عمومی با نام:
```
عنوان: اعلان با نام
متن: کاربر گرامی {0}، {1}
```

**استفاده:**
```python
pattern_args=["علی احمدی", "سفارش شما ثبت شد"]
```

---

## ⚠️ نکات مهم:

1. **Pattern باید تایید شود:** تا Pattern تایید نشود، نمی‌توانید از آن استفاده کنید
2. **متن Pattern:** می‌توانید Pattern را به هر شکلی که می‌خواهید بنویسید، فقط باید `{0}` برای متن پیام داشته باشید
3. **هزینه:** Pattern معمولاً ارزان‌تر از پیامک آزاد است

---

## 🔍 عیب‌یابی:

**اگر Pattern کار نکرد:**
1. مطمئن شوید Pattern تایید شده است
2. Pattern ID را در `.env` بررسی کنید
3. لاگ‌های سرور را بررسی کنید

**لاگ‌های مفید:**
```
Attempting to send via Pattern ID: 389656
Pattern failed: ... Falling back to free-form SMS.
```

---

## ✅ مزایای Pattern:

- ✅ ارزان‌تر
- ✅ تحویل بهتر
- ✅ بدون نیاز به فعال‌سازی شماره فرستنده
- ✅ مناسب برای اعلان‌های استاندارد

---

## 📞 پشتیبانی:

اگر مشکلی داشتید:
1. لاگ‌های سرور را بررسی کنید
2. Pattern ID را در پنل MeliPayamak بررسی کنید
3. مطمئن شوید Pattern تایید شده است

