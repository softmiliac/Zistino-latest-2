# 📱 راهنمای ثبت Pattern در MeliPayamak

## 🎯 Pattern پیشنهادی

### Pattern ساده و انعطاف‌پذیر (⭐ توصیه می‌شود)

**عنوان Pattern:**
```
پیام عمومی زیستینو
```

**متن Pattern:**
```
{0}
```

**توضیحات:**
- ✅ فقط یک متغیر دارد: `{0}`
- ✅ می‌تواند هم کد OTP باشد و هم پیام متنی
- ✅ ساده و انعطاف‌پذیر
- ✅ کوتاه (فضای بیشتری برای پیام)

**مثال استفاده:**
- برای OTP: `123456` → پیامک: `123456`
- برای اعلان: `سفارش شما ثبت شد` → پیامک: `سفارش شما ثبت شد`

---

## 📝 مراحل ثبت Pattern در پنل MeliPayamak

### مرحله 1: ورود به پنل

1. به آدرس https://panel.payamak-panel.com/ بروید
2. با نام کاربری و رمز عبور خود وارد شوید

### مرحله 2: رفتن به بخش Pattern ها

1. از منوی سمت راست، روی **"الگوها"** یا **"Patterns"** کلیک کنید
2. یا به مسیر: **پیامک** → **الگوها** بروید

### مرحله 3: ایجاد Pattern جدید

1. روی دکمه **"ایجاد الگوی جدید"** یا **"افزودن الگو"** کلیک کنید
2. فرم ثبت Pattern را پر کنید:

**فیلدهای فرم:**

| فیلد | مقدار | توضیحات |
|------|-------|---------|
| **عنوان** | `پیام عمومی زیستینو` | نام Pattern |
| **متن Pattern** | `{0}` | متن Pattern با متغیر |
| **نوع Pattern** | `اعلان` یا `عمومی` | نوع Pattern |
| **Black List ID** | `1` | شناسه لیست سیاه (معمولاً 1) |

### مرحله 4: ثبت و تایید

1. روی دکمه **"ثبت"** یا **"ذخیره"** کلیک کنید
2. Pattern شما ثبت می‌شود و در وضعیت **"در انتظار تایید"** قرار می‌گیرد
3. معمولاً **1-2 روز کاری** طول می‌کشد تا Pattern تایید شود

### مرحله 5: دریافت Pattern ID

بعد از تایید Pattern:
1. به لیست Pattern ها برگردید
2. Pattern خود را پیدا کنید
3. **Pattern ID** را یادداشت کنید (مثلاً `123456`)

---

## 🔧 استفاده از Pattern در کد

### 1. اضافه کردن Pattern ID به .env

فایل `.env` را باز کنید و این خط را اضافه کنید:

```env
# MeliPayamak Pattern ID (برای OTP و اعلان‌ها)
MELIPAYAMAK_PATTERN_ID=123456
```

(عدد `123456` را با Pattern ID خودتان جایگزین کنید)

همچنین مطمئن شوید که این تنظیمات هم وجود دارند:

```env
# MeliPayamak REST API
MELIPAYAMAK_USERNAME=09155009664
MELIPAYAMAK_API_KEY=f2b89fd9-5633-4623-92c1-b3a896639f76
MELIPAYAMAK_API_URL=https://rest.payamak-panel.com/api/SendSMS/SendSMS
MELIPAYAMAK_SENDER=30008666009664
```

### 2. استفاده در کد

بعد از restore کردن فایل `sms_service.py`، می‌توانید از تابع `send_universal_pattern_sms` استفاده کنید:

```python
from zistino_apps.payments.sms_service import send_universal_pattern_sms

# برای OTP
success, error = send_universal_pattern_sms("09123456789", "123456")

# برای پیام متنی
success, error = send_universal_pattern_sms("09123456789", "سفارش شما ثبت شد")
```

---

## 📊 نحوه کار Pattern

### برای کد OTP:
```
ورودی: "123456"
Pattern: "{0}"
خروجی: "123456"
```

### برای پیام متنی:
```
ورودی: "سفارش شما ثبت شد"
Pattern: "{0}"
خروجی: "سفارش شما ثبت شد"
```

---

## ⚠️ نکات مهم

1. **Pattern باید تایید شود:** تا Pattern تایید نشود، نمی‌توانید از آن استفاده کنید
2. **متغیرها:** Pattern باید فقط یک متغیر داشته باشد: `{0}`
3. **طول پیام:** Pattern + پیام نباید از 160 کاراکتر بیشتر شود
4. **تست:** بعد از تایید Pattern، حتماً تست کنید

---

## 🔍 عیب‌یابی

### مشکل: Pattern تایید نشده
- **راه‌حل:** منتظر بمانید تا Pattern توسط MeliPayamak تایید شود (1-2 روز کاری)

### مشکل: Pattern ID اشتباه
- **راه‌حل:** Pattern ID را در پنل MeliPayamak بررسی کنید

### مشکل: پیامک ارسال نمی‌شود
- **راه‌حل:** 
  1. لاگ‌های سرور را بررسی کنید
  2. Pattern ID را در `.env` بررسی کنید
  3. مطمئن شوید Pattern تایید شده است

---

## 📞 پشتیبانی

اگر مشکلی داشتید:
1. لاگ‌های سرور را بررسی کنید
2. Pattern ID را در پنل MeliPayamak بررسی کنید
3. مطمئن شوید Pattern تایید شده است

---

## ✅ خلاصه

1. **Pattern را در پنل MeliPayamak ثبت کنید:**
   - عنوان: `پیام عمومی زیستینو`
   - متن: `{0}`

2. **منتظر تایید بمانید** (1-2 روز کاری)

3. **Pattern ID را در `.env` اضافه کنید:**
   ```env
   MELIPAYAMAK_PATTERN_ID=YOUR_PATTERN_ID
   ```

4. **از تابع `send_universal_pattern_sms` استفاده کنید**

5. **تست کنید!**

