# 📱 راهنمای وضعیت Pattern در MeliPayamak

## ✅ Pattern شما ثبت شده است!

از تصویر می‌بینم که Pattern شما با موفقیت ثبت شده:

### 📋 اطلاعات Pattern:

| فیلد | مقدار |
|------|-------|
| **Pattern ID** | `395` |
| **عنوان** | `زیستینو` |
| **متن Pattern** | `{0}` ✅ |
| **تاریخ ثبت** | `۱۴۰۴/۰۸/۲۷` - `۱۳:۱۶` |
| **وضعیت** | `نیاز به ویرایش` ⚠️ |

---

## ⚠️ وضعیت "نیاز به ویرایش"

وضعیت **"نیاز به ویرایش"** به این معنی است که:

1. ✅ Pattern شما ثبت شده است
2. ⏳ Pattern در حال بررسی توسط MeliPayamak است
3. 📝 ممکن است نیاز به ویرایش یا تایید داشته باشد

### مراحل بعدی:

#### گزینه 1: منتظر بمانید (توصیه می‌شود)
- معمولاً 1-2 روز کاری طول می‌کشد تا Pattern تایید شود
- بعد از تایید، وضعیت به **"تایید شده"** یا **"فعال"** تغییر می‌کند

#### گزینه 2: بررسی کنید
- روی Pattern کلیک کنید و ببینید آیا پیامی از MeliPayamak هست
- ممکن است نیاز به ویرایش توضیحات یا عنوان باشد

---

## 🔧 تنظیمات در .env

حالا که Pattern ID را دارید (`395`)، آن را در فایل `.env` اضافه کنید:

```env
# MeliPayamak Pattern ID (برای OTP و اعلان‌ها)
MELIPAYAMAK_PATTERN_ID=395
```

همچنین مطمئن شوید که این تنظیمات هم وجود دارند:

```env
# MeliPayamak REST API
MELIPAYAMAK_USERNAME=09155009664
MELIPAYAMAK_API_KEY=f2b89fd9-5633-4623-92c1-b3a896639f76
MELIPAYAMAK_API_URL=https://rest.payamak-panel.com/api/SendSMS/SendSMS
MELIPAYAMAK_SENDER=30008666009664
```

---

## 📝 استفاده از Pattern

بعد از تایید Pattern (وقتی وضعیت به "تایید شده" یا "فعال" تغییر کرد):

### 1. ری‌استارت سرور
```bash
# Stop server (Ctrl+C)
python manage.py runserver
```

### 2. تست Pattern

#### تست OTP:
```bash
curl -X POST http://127.0.0.1:8000/api/v1/auth/send-code/ \
  -H "Content-Type: application/json" \
  -d '{"phone_number": "09017021166"}'
```

#### تست اعلان:
```bash
curl -X POST http://127.0.0.1:8000/api/v1/notifications/send \
  -H "Content-Type: application/json" \
  -H "Authorization: Token YOUR_TOKEN" \
  -d '{
    "phoneNumber": "09017021166",
    "message": "سفارش شما ثبت شد"
  }'
```

---

## ⚠️ نکات مهم

1. **منتظر تایید بمانید:** تا Pattern تایید نشود، نمی‌توانید از آن استفاده کنید
2. **بررسی منظم:** هر چند ساعت یکبار وضعیت Pattern را در پنل بررسی کنید
3. **Pattern ID را ذخیره کنید:** Pattern ID `395` را در `.env` اضافه کنید

---

## 🔍 عیب‌یابی

### اگر Pattern تایید نشد:
1. روی Pattern کلیک کنید و ببینید آیا پیامی از MeliPayamak هست
2. ممکن است نیاز به ویرایش توضیحات باشد
3. با پشتیبانی MeliPayamak تماس بگیرید

### اگر Pattern رد شد:
1. Pattern جدیدی با توضیحات بهتر ثبت کنید
2. مطمئن شوید Pattern فقط یک متغیر دارد: `{0}`

---

## ✅ خلاصه

1. ✅ Pattern با ID `395` ثبت شده
2. ⏳ منتظر تایید MeliPayamak بمانید (1-2 روز کاری)
3. 📝 Pattern ID را در `.env` اضافه کنید: `MELIPAYAMAK_PATTERN_ID=395`
4. 🔄 بعد از تایید، Pattern آماده استفاده است

---

## 📞 پشتیبانی

اگر بعد از 2-3 روز Pattern تایید نشد:
1. با پشتیبانی MeliPayamak تماس بگیرید
2. وضعیت Pattern را بررسی کنید
3. ممکن است نیاز به ویرایش باشد

